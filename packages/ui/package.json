{
  "name": "ui",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=8.11.1"
  },
  "scripts": {
    "build": "gulp",
    "build:server": "yarn run build",
    "build:desktop": "yarn run build",
    "watch": "gulp launch",
    "watch:server": "yarn run watch",
    "watch:desktop": "gulp watch",
    "clean": "gulp clean"
  },
  "dependencies": {
    "protobufjs": "^6.8.6"
  },
  "devDependencies": {
    "@types/node": "^8.5.2",
    "@types/websocket": "0.0.38",
    "browser-sync": "^2.23.6",
    "browserify": "^16.2.2",
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-download-stream": "0.0.19",
    "protobuf-templates": "^1.0.6",
    "tsify": "^4.0.0",
    "typescript": "^2.6.2",
    "vinyl-source-stream": "^2.0.0"
  }
}
