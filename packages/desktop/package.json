{
  "name": "desktop",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": ">=8.11.1"
  },
  "scripts": {
    "build": "gulp",
    "build:desktop": "yarn run build",
    "desktop": "gulp copy && electron .",
    "watch": "gulp watch",
    "watch:desktop": "yarn run watch",
    "clean": "gulp clean"
  },
  "dependencies": {
    "electron": "^1.8.4",
    "protobufjs": "^6.8.6"
  },
  "devDependencies": {
    "@types/node": "^8.5.2",
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-download-stream": "0.0.19",
    "gulp-run-electron": "^3.0.2",
    "gulp-typescript": "^3.2.3",
    "protobuf-templates": "^1.0.6",
    "typescript": "^2.6.2"
  }
}
